Задача: Cow Gymnastics
Теги: #complete_search #map
Возникшие вопросы:
1. КАК нужно было объявлять вектор из векторов ?
  Ответ: vector<vector<int>> v(n, vector<int>(m));
2. КАК пройтись по map, где ключом является pair ?
  Ответ:  for (const auto& [key, value] : my_map)
  
Ошибка: в пары добвляла не значения cow_map[{cows[i][j], cows[i][o]}]++,
A их индексы  cow_map[{j, o}]++;
Описание:
Прикольная задачка, очень обрадовала меня тем, что я ее смогла решить впервые за несколько дней застоя.
Весь алгоритм в том, чтобы через 3 цикла считывать пары в map и прибавлять их значение, а в конце
просто сравнить их значения с количеством строк K.
Естественно, у меня не самое оптимальное решение, но меня это не волнует!

Код:
#include <bits/stdc++.h>
using namespace std;
int main() {
freopen("gymnastics.in", "r", stdin);
freopen("gymnastics.out", "w", stdout);
  int k, n;
  cin >> k >> n;
  vector<vector<int>> cows(k, vector<int>(n));
  for (int i=0; i<k; i++) {
      for (int j=0; j<n; j++) {
          cin >> cows[i][j];
      }
  }
  map<pair<int, int>, int> cow_map;
  for (int i=0; i<k; i++) {
      for (int j=0; j<n; j++) {
          for (int o=j+1; o<n; o++) {
              cow_map[{cows[i][j], cows[i][o]}]++;
          }
      }
    }
    int ans=0;
    for (const auto& [key, value] : cow_map) {
        if (value==k) ans++;
    }
    cout << ans;
}

